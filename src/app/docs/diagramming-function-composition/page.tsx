import { DocsLayout } from '@/components/DocsLayout'

export default function Page() {
	return (
		<DocsLayout frontmatter={{ title: "Diagramming Function Composition", description: "" }} nodes={[]}>
		<p>This is a template page.</p>
		</DocsLayout>
	)
}